name: Keep Aura Bloom awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: curl -sf https://aura-bloom-dgi7.onrender.com/ -o /dev/null && echo "OK"
